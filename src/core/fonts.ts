import { Open_Sans, Sono, Work_Sans } from 'next/font/google';

export const openSans = Open_Sans({ subsets: ['latin'] });
export const workSans = Work_Sans({
  subsets: ['latin'],
  weight: ['300', '400', '500', '700'],
});
export const sono = Sono({
  subsets: ['latin'],
  weight: ['300', '400', '500', '700'],
});
